1. Create an IAM user account and assign a police 

2. Create a new NAT gateway and a new Elastic IP address, and assign it to the newly created NAT gateway.

3. Display the Elastic IP (EIP), the user_name arn & the NAT gateway ID on your terminal screen.

4. Destroy all your Terraform-deployed infrastructure.