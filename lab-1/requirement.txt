1. Create an IAM user that has programmatic access with the appropriate permissions to create EC2 instances, VPCs, and subnets. Use this user to do the following tasks.

2. Create a new VPC and a new public subnet, using any supported CIDR blocks.

3. Create an EC2 instance of t2.micro type and assign a tag to it as follows: name "dolfined_instance", ensure the instance will be assigned a public IP.

4. Ensure the EC2 instance is created inside the newly created VPC public subnet above.